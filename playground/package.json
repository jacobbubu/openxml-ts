{
  "name": "openxml-ts-playground",
  "version": "0.0.0",
  "private": true,
  "description": "Browser playground for openxml-ts; drop docx/xlsx/pptx, edit element tree, download.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "openxml-ts": "file:.."
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^5.4.10"
  }
}
